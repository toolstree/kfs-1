
.. _program_listing_file_lib_format_src_format.cpp:

Program Listing for File format.cpp
===================================

|exhale_lsh| :ref:`Return to documentation for file <file_lib_format_src_format.cpp>` (``lib/format/src/format.cpp``)

.. |exhale_lsh| unicode:: U+021B0 .. UPWARDS ARROW WITH TIP LEFTWARDS

.. code-block:: cpp

   //
   // Created by pierre on 12/18/25.
   //
   
   #include "format/format.hpp"
   
   char * format::format(char *buffer, int value) {
       if (value < 0) {
           value = -value;
           *(buffer++) = '-';
       }
   
       return format(buffer, static_cast<uint32_t>(value));
   }
   
   char * format::format(char *buffer, uint32_t value) {
       char buffer_tmp[10] = {};
       char *start = buffer_tmp + 9;
   
       do {
           *(--start) = '0' + (value % 10);
           value /= 10;
       } while (value != 0);
   
       while (start < buffer_tmp + 9) {
           *(buffer++) = *(start++);
       }
   
       return buffer;
   }
   
   char * format::format(char *buffer, const char *value) {
       auto len = kstring::strlen(value);
       kstring::memcpy(buffer, value, len);
   
       return buffer + len;
   }
